[nitpick.styles]
include = [
    "../mypy/mypy-3.11.toml",
]

[[".pre-commit-config.yaml".repos]]
repo = "https://github.com/asottile/pyupgrade"
hooks = [{id = "pyupgrade", args = ["--py311-plus"]}]

["pyproject.toml".project]
requires-python = ">=3.11"
